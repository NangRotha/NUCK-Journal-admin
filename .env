# Local development — use this when running the backend on your machine
# VITE_API_URL=http://127.0.0.1:8000

# Production (deployed on Vercel) — must point to the Railway backend
VITE_API_URL=https://nuck-journal-backend-production.up.railway.app